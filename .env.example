COMPOSE_PROJECT_NAME=lakehouse

# Generate with: docker compose run --rm kafka kafka-storage random-uuid
KAFKA_CLUSTER_ID=REPLACE_ME

# MinIO object store credentials (S3-compatible)
MINIO_ROOT_USER=lakehouse
MINIO_ROOT_PASSWORD=lakehouse123
